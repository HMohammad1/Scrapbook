<script type ="text/javascript" src="/js/post.js"></script>
<link rel="stylesheet" href="/css/post.css">

<div class="map-overlay">
    <div class="post-holder no-scroll-bar" id="<%= post.postID %>">
        <div class="container p-0">
            <div class="row">
                <div class="col post-heading">
                    <%= post.title %>
                </div>
            </div>
            <div class="row img-holder-main carousel slide p-1">
                <div class="col">
                    <div class="carousel-inner">
                        <% if (post.media) { %>
                            <% post.media.forEach(src => { %>
                                <div class="carousel-item active">
                                    <img src="<%= src %>" style="height: 695px" class="d-block w-100" alt="...">
                                </div>
                            <% }) %>
                        <% } %>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample"
                            data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carouselExample"
                            data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                </div>
            </div>
            <div class="row post-reactions p-4">
                <%- include('../post/reactions', {reacts: post.reacts, user: user}); %>
            </div>
            <div class="row post-desc pt-5">

                <div class="col">
                    <div class="row">
                        <div class="col px-3">
                            <%= post.desc %>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col">
                            <div class="row add-comment">
                                <div class="col">
                                    <div class="row add-comment">
                                        <textarea rows="4" cols="70" type="text" name="comment" id="comment" placeholder="Add a comment" class="form-control comment-input"></textarea>
                                    </div>
                                    <div class="row comment-btn-holder">
                                        <button class="btn comment-btn cancel" disabled="disabled">Cancel</button>
                                        <button class="btn comment-btn add" disabled="disabled">Post</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row all-comments p-3">
                <div class="col">
                    <div class="post-comments">
                        <div class="spinner-border text-danger" role="status">
                            <span class="sr-only">Loading comments</span>
                        </div>
                    </div>
                </div>

            </div>

        </div>

    </div>
</div>

